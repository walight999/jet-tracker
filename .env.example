# ── Required for alerts to go out ─────────────────────
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── Tunables ──────────────────────────────────────────
POLL_INTERVAL_MIN=5
ALERT_COOLDOWN_HOURS=6

# ── Optional: Notion log (skips if empty) ─────────────
# Create a Notion DB with the schema listed in notion_log.py
NOTION_TOKEN=
NOTION_DB_ID=

# ── Optional: GoldBot signal forwarder (skips if empty) ─
# POST endpoint that ingests XAU-relevant flight alerts.
# Wire to your existing Make scenario or a new webhook.
GOLD_SIGNAL_WEBHOOK=
